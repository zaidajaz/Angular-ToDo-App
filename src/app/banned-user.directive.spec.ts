import { BannedUserDirective } from './banned-user.directive';

describe('BannedUserDirective', () => {
  it('should create an instance', () => {
    const directive = new BannedUserDirective();
    expect(directive).toBeTruthy();
  });
});
